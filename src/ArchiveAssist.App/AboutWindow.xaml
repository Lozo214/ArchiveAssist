<Window x:Class="ArchiveAssist.App.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Archive Assist" Width="470" Height="315" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="#F4F6F8" ShowInTaskbar="False">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Archive Assist" FontSize="28" FontWeight="SemiBold" Foreground="#17212B" />
        <TextBlock x:Name="VersionText" Grid.Row="1" Margin="0,5,0,0" Foreground="#5D6873" />

        <StackPanel Grid.Row="2" Margin="0,22,0,0">
            <TextBlock TextWrapping="Wrap" Foreground="#2F3A44"
                       Text="A non-destructive PDF archive workflow assistant for production reporting, searchable-copy OCR, and PDF page equalization." />
            <TextBlock Margin="0,16,0,0" TextWrapping="Wrap" Foreground="#5D6873"
                       Text="Archive Assist keeps source files unchanged and writes generated files to separately selected output folders." />
        </StackPanel>

        <Button Grid.Row="3" HorizontalAlignment="Right" MinWidth="95" Padding="16,7"
                Content="Close" IsCancel="True" IsDefault="True" />
    </Grid>
</Window>
